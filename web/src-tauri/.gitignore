# Separate Cargo project from the root workspace; not covered by the root
# /target ignore rule since that's anchored to the repo root.
target/

# Generated by the Tauri CLI (capability schema cache); regenerate, don't commit.
gen/schemas/
