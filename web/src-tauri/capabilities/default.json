{
  "$schema": "https://schema.tauri.app/config/2",
  "identifier": "default",
  "description": "Baseline window capability for the spike's single window. The save/load filesystem commands (web/src-tauri/src/main.rs) are plain app-defined commands, not a permissioned plugin, so they need no extra entry here.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
